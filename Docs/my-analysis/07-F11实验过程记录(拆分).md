# F11 实验过程记录(Zviolin · 8/21 基线后新增 · 拆分版)

> **用途**:按《增量验收细则》§4「增量验收方法」记录 8/21 公共基线(`acce839c`)之后的**真实新增实验过程**——代码增量 / 实验增量 / 知识增量三层证据链。
> **拆分思路**(参考 `practices/DATA/docs/04-08` 与 `Data/docs/06-11` 的步骤拆分模式):每个实验任务 = 一节 = 配置 / 命令 / 日志 / 结果 / 结论五段。每个实验再拆为子任务,子任务独立记录。
>
> **文档版本**:v2.0(2026-08-23)｜**Owner**:张伟林(Zviolin)｜**基线**:`acce839c7e895d6b179de7f7093fa879e237cc7b`
> **原则**:每个实验任务 = 配置/manifest/原始日志/指标表 四件套 + 独立可复现

---

## 目录

1. [实验任务拆分总览](#一实验任务拆分总览)
2. [T0:锁定基线 + 环境复现 + Smoke](#二t0smoke)
3. [T1:教师特征缓存工具(P0)](#三t1p0)
4. [T2:q_teacher 构造(P0/P1)](#四t2p0p1)
5. [T3:Router KD 训练闭环(P0)](#五t3p0)
6. [T4:三方同预算对照(P1)](#六t4p1)
7. [T5:路由行为分析 + 零教师证明(P1)](#七t5p1)
8. [T6:扩展实验(P2 可选)](#八t6p2)
9. [增量汇总表](#九增量汇总表)

---

## 一、实验任务拆分总览

> **拆分依据**:官方任务书 F11 章节 + 筹备会 Slide 22(D 主题)+ 增量验收细则 §4 三层证据。
> **每个任务独立**:配置 / manifest / 原始日志 / 指标表 / 结论 五段格式(参考 practices/DATA/docs/04-08 步骤思路)。

| 任务 | 内容 | 对应级别 | 验收锚点 | 独立小节 | 子任务数 |
|------|------|---------|---------|---------|---------|
| T0 | 锁定基线 + 环境复现 + Smoke | Smoke | 环境可复现、基线跑通 | §二 | 5 |
| T1 | 教师特征缓存工具 | P0 | 缓存可复现(manifest+SHA)| §三 | 5 |
| T2 | q_teacher 构造 | P0/P1 | 熵非退化、非均匀常数 | §四 | 6 |
| T3 | Router KD 训练闭环 | P0 | 梯度非零、任务 loss 下降 | §五 | 5 |
| T4 | 三方同预算对照 | P1 | baseline/表征KD/RouterKD 对照表 | §六 | 7 |
| T5 | 路由行为分析 + 零教师证明 | P1 | top-k 一致率/熵/负载 + 移除证明 | §七 | 4 |
| T6 | 扩展实验(可选)| P2 | 多层/MoT/跨域/教师对比 | §八 | 5 |

> ⚠️ **所有时间戳必须在 2026-08-21 23:59:59 之后**,否则属历史成果(登记到 06 表,不计 P1/P2)。

---

## 二、T0:锁定基线 + 环境复现 + Smoke

### T0.1 同步上游并切换 commit

**配置**

| 项 | 值 |
|----|-----|
| 日期 | ⏳ 待记录(须 ≥ 2026-08-22)|
| 上游 | `Tencent/YOLO-Master` |
| 锁定 commit | `acce839c7e895d6b179de7f7093fa879e237cc7b` |
| 本地工作分支 | `f11-baseline` |
| 设备 | RTX 5060 Laptop(8GB)|

**命令**

```powershell
cd G:\Codes\OpenSource\Rhino-bird\practices\\Codes\\YOLO-Master
git fetch upstream
git checkout acce839c7e895d6b179de7f7093fa879e237cc7b
git checkout -b f11-baseline
git log -1 --format="%H %s"
```

**日志与产物**

- [ ] HEAD 输出:`acce839c7e895d6b179de7f7093fa879e237cc7b`(任务书锁定)
- [ ] 分支列表含 `f11-baseline`

**结论**:⏳(通过 → 进入 T0.2;失败 → 改用 fork 内历史 commit 并书面说明)

---

### T0.2 验证环境

**配置**

| 项 | 期望 |
|----|------|
| Python | 3.10.9 |
| PyTorch | 2.9.1+cu130 |
| ultralytics | 8.3.240(editable)|
| CUDA | 13.0 |

**命令**

```powershell
conda activate yolo-master
python -c "import torch, ultralytics; print(torch.__version__, ultralytics.__version__)"
pip show ultralytics | grep Location
```

**日志与产物**

- [ ] 三个版本号输出
- [ ] Location 指向 `practices/Codes/YOLO-Master`

**结论**:⏳(通过 → T0.3;不通过 → 重新 `pip install -e .`)

---

### T0.3 detect 基线 smoke

**配置**

| 项 | 值 |
|----|-----|
| 模型 | `ultralytics/cfg/models/master/v0_8/det/yolo-master-n.yaml` |
| 数据 | `coco8.yaml` |
| epochs | 1 |
| imgsz | 640 |
| batch | 8 |
| device | 0 |
| project | `runs/smoke` |

**命令**

```powershell
python -c "
from ultralytics import YOLO
m = YOLO('ultralytics/cfg/models/master/v0_8/det/yolo-master-n.yaml')
m.train(data='coco8.yaml', epochs=1, imgsz=640, batch=8, device=0, project='runs/smoke')
print('SMOKE OK')
"
```

**日志与产物**

- [ ] 训练日志路径:`runs/smoke/train/results.csv`
- [ ] box / cls / dfl 三条 loss 曲线均下降
- [ ] 显存峰值 ≤ 6.5GB
- [ ] mAP50-95 数值(冒烟不要求 > baseline)

**结论**:⏳(通过 → T0.4;OOM → batch=4→2 重试)

---

### T0.4 30/30 边界测试

**命令**

```powershell
pytest tests/test_mot.py tests/test_moa.py -v > runs/smoke/test_results.txt
```

**日志与产物**

- [ ] test_results.txt 含 30+ 通过记录
- [ ] 无 failure / error(允许 xfail 1 个,符合 v26.08 已知状态)

**结论**:⏳(30/30 通过 → Smoke 完成;失败 → 记录失败 ID 作为 F11 增量 PR 候选)

---

### T0.5 Smoke 总结

| 项 | 状态 |
|----|------|
| 锁定 commit | ⏳ |
| 环境 | ⏳ |
| detect smoke | ⏳ |
| 边界测试 | ⏳ |
| **Smoke 通过** | ⏳(全部 ✅ → 进入 T1)|

---

## 三、T1:教师特征缓存工具(P0)

### T1.1 教师模型加载

**配置**

| 项 | 值 |
|----|-----|
| 模型 | `facebook/dinov2-base`(ViT-B/14)|
| 加载方式 | HuggingFace `AutoModel.from_pretrained(...)` |
| 冻结 | `requires_grad=False`(全部参数)|

**命令**

```powershell
python -c "
import torch
from transformers import AutoModel
m = AutoModel.from_pretrained('facebook/dinov2-base')
for p in m.parameters(): p.requires_grad = False
print('Teacher loaded:', sum(p.numel() for p in m.parameters()) / 1e6, 'M params')
"
```

**日志与产物**

- [ ] 教师参数量 ~86M(ViT-B/14 基线)
- [ ] `requires_grad` 全 False 验证

**结论**:⏳

---

### T1.2 数据采样与清单

**配置**

| 项 | 值 |
|----|-----|
| 数据 | coco8(或 COCO val 子集 100 张)|
| 数量 | 100(可配置 200/500)|
| 划分 | 训练集前 100 张 |

**命令**

```powershell
python scripts/prepare_coco_mini.py --num 100 --out runs/f11_teacher_cache/data_manifest.json
```

**日志与产物**

- [ ] data_manifest.json 含 100 个文件名 + SHA256

**结论**:⏳

---

### T1.3 离线前向 + 分片缓存

**配置**

| 项 | 值 |
|----|-----|
| 输入 | 224×224 RGB(教师原始)|
| 输出 | patch 特征(N, 768)float16 |
| 分片 | 每片 25 张 → 4 片 |
| batch | 4 |

**命令**

```powershell
python scripts/cache_teacher_features.py \
  --model facebook/dinov2-base \
  --data runs/f11_teacher_cache/data_manifest.json \
  --batch 4 --imgsz 224 \
  --out runs/f11_teacher_cache \
  --device 0
```

**日志与产物**

- [ ] 4 个 shard NPY 文件
- [ ] 每片 < 2GB
- [ ] manifest.json(顶层)含 SHA256 + 模型 ID + 复现命令 + 时间戳

**结论**:⏳(可复现 ✅ → T1.4)

---

### T1.4 复现性验证

**命令**

```powershell
# 删除缓存后按 README 复现
Remove-Item -Recurse runs/f11_teacher_cache/shards
python scripts/cache_teacher_features.py --config runs/f11_teacher_cache/manifest.json
python scripts/verify_cache.py --cache runs/f11_teacher_cache
# 应输出:SHA256 一致
```

**日志与产物**

- [ ] verify_cache.py 报告 SHA 一致

**结论**:⏳(✅ → T1.5)

---

### T1.5 README 与复现包雏形

**产物**

- [ ] `runs/f11_teacher_cache/README.md`:从零复现说明
- [ ] manifest.json 含完整命令

**结论**:⏳(✅ → T1 完成,进入 T2)

---

### T1 增量总结

| 层 | 增量 |
|----|------|
| 代码增量 | `scripts/cache_teacher_features.py` + `scripts/verify_cache.py` + 文档 |
| 实验增量 | 100 张 DINOv2 教师特征 4 片缓存 + manifest + SHA |
| 知识增量 | 8GB 单卡 DINO ViT-B/14 离线缓存方法论 + 显存/磁盘占用记录 |

---

## 四、T2:q_teacher 构造(P0/P1)

### T2.1 专家输出提取

**配置**

| 项 | 值 |
|----|-----|
| 学生模型 | yolo-master-n(单路由层、2 专家最小配置)|
| 提取位置 | Router logits,shape (B, 2) |
| 训练 | T0.3 smoke 模型权重 |

**命令**

```powershell
python scripts/extract_expert_outputs.py \
  --model yolo-master-n.yaml \
  --weights runs/smoke/train/weights/best.pt \
  --data coco8.yaml --batch 8 \
  --out runs/f11_expert_outputs/
```

**日志与产物**

- [ ] expert_outputs.npy(B, 2) × 100 张
- [ ] 分布直方图 PNG(目标:非单峰)

**结论**:⏳

---

### T2.2 一致性度量

**配置**

| 项 | 值 |
|----|-----|
| 度量 | 余弦相似度 cos_sim(E_i, T)|
| 教师特征 | T1.3 缓存 |
| patch 选择 | mean pooling → (1, 768)|

**命令**

```powershell
python scripts/compute_consistency.py \
  --expert-outputs runs/f11_expert_outputs/ \
  --cache runs/f11_teacher_cache/ \
  --out runs/f11_consistency/
```

**日志与产物**

- [ ] logit 矩阵 (100, 2)
- [ ] 数值范围 [-1, 1]

**结论**:⏳(数值合理 → T2.3)

---

### T2.3 温度 + 掩码

**配置**

| 项 | 默认 | 搜索空间 |
|----|------|----------|
| τ | 1.0 | {0.5, 1.0, 2.0} |
| τ_mask | 0.0 | {0.0, 0.5, 1.0} |

**命令**

```powershell
python scripts/apply_temperature_mask.py \
  --consistency runs/f11_consistency/ \
  --tau 1.0 --tau-mask 0.0 \
  --out runs/f11_q_teacher_raw/
```

**日志与产物**

- [ ] q_teacher_raw.npy (100, 2)
- [ ] 熵值日志 H ∈ [-log 2, 0]

**结论**:⏳(继续 T2.4)

---

### T2.4 平滑混合

**配置**

| 项 | 默认 | 搜索空间 |
|----|------|----------|
| ε | 0.1 | {0.05, 0.1, 0.2} |

**命令**

```powershell
python scripts/smooth_q_teacher.py \
  --q-raw runs/f11_q_teacher_raw/ \
  --eps 0.1 \
  --out runs/f11_q_teacher/
```

**日志与产物**

- [ ] q_teacher.npy (100, 2)
- [ ] 平滑前后熵值对比

**结论**:⏳(进入 T2.5)

---

### T2.5 熵与非退化验证

**配置**

| 指标 | 目标 |
|------|------|
| H_norm | ∈ (0.3, 0.8)|
| max(q) | < 0.9 |
| top-1 切换率 | ∈ (0.2, 0.8) |

**命令**

```powershell
python scripts/verify_q_teacher.py --q runs/f11_q_teacher/ --out runs/f11_q_teacher_check/
```

**日志与产物**

- [ ] H_norm 数值 + 曲线
- [ ] 分布直方图 PNG
- [ ] top-1 切换率统计

**结论**:⏳(全部达标 → T2.6;否则调 τ/ε/掩码 重做 T2.3)

---

### T2.6 配置文件记录

**产物**

- [ ] `configs/f11_q_teacher.yaml`:完整参数(τ, ε, τ_mask, 验证结果)
- [ ] 与 #54 `compare_routing_synthetic_vs_real.py` 合成 vs 真实对比结果归档

**结论**:⏳(✅ → T2 完成,进入 T3)

---

### T2 增量总结

| 层 | 增量 |
|----|------|
| 代码增量 | 5 个脚本(extract / consistency / temp+mask / smooth / verify)+ 配置 yaml |
| 实验增量 | 100 张样本的 q_teacher 分布 + 验证报告 |
| 知识增量 | 温度/平滑/掩码三参数对分布形态的影响曲线(供 P1 论文)|

---

## 五、T3:Router KD 训练闭环(P0)

### T3.1 distill criterion

**配置**

| 项 | 值 |
|----|-----|
| 损失类型 | JS(默认)/ KL(可切换)|
| 温度 | 1.0 |
| 平滑 | 0.1 |
| 文件 | `ultralytics/nn/loss/router_kd.py` |

**命令**

```powershell
# 实现 RouterKDLoss + 单测
pytest tests/test_router_kd.py -v
```

**日志与产物**

- [ ] 单测通过(loss 数值在已知输入下符合 JS/KL 公式)
- [ ] 参数量 = 0(loss 类不引入参数)

**结论**:⏳

---

### T3.2 collect_aux_loss 注册

**配置**

| 项 | 值 |
|----|-----|
| 注册 kind | `router_kd` |
| 位置 | `ultralytics/utils/loss.py` collect_aux_loss 白名单 |

**命令**

```powershell
pytest tests/test_collect_aux_loss.py -v
# 测试: RouterKDLoss 加入 criterion 后, aux_loss['router_kd'] 非零
```

**日志与产物**

- [ ] 注册前后辅助损失日志对比(从 0 → 非 0)

**结论**:⏳

---

### T3.3 单路由层配置

**配置**

| 项 | 值 |
|----|-----|
| 模型 | yolo-master-n 修改版 |
| layers | 1(单路由层)|
| experts | 2 |

**命令**

```powershell
python -c "
from ultralytics import YOLO
m = YOLO('configs/f11_router_kd_n_l1e2.yaml')
print('Model loaded:', m.model.args)
"
```

**日志与产物**

- [ ] 模型参数量打印
- [ ] Router 层数与专家数核对

**结论**:⏳

---

### T3.4 单 batch 训练(冒烟)

**配置**

| 项 | 值 |
|----|-----|
| 数据 | coco8 |
| epochs | 1 |
| batch | 8 |
| imgsz | 640 |
| λ | 0.5 |
| warmup | 5 epoch |

**命令**

```powershell
python scripts/smoke_router_kd.py \
  --config configs/f11_router_kd_smoke.yaml \
  --epochs 1 --device 0 \
  --project runs/f11_smoke_kd/
```

**日志与产物**

- [ ] 训练日志含 KD loss / task loss
- [ ] KD loss 在 1 epoch 内下降
- [ ] 任务 loss 正常下降

**结论**:⏳(✅ → T3.5;loss 不降 → 检查 criterion / λ / 注册)

---

### T3.5 梯度非零检查

**命令**

```powershell
python scripts/check_grad_router_kd.py \
  --config configs/f11_router_kd_smoke.yaml
# 输出: router.* 参数 grad norm 全部 > 0
```

**日志与产物**

- [ ] grad norm 日志(每层 router 的 grad L2 范数)
- [ ] 截图归档

**结论**:⏳(全部 > 0 → T3 完成;为 0 → 重新查注册/criterion)

---

### T3 增量总结

| 层 | 增量 |
|----|------|
| 代码增量 | RouterKDLoss + collect_aux_loss 注册 + 单路由层配置 + 单测 + smoke 脚本 |
| 实验增量 | 冒烟训练日志 + grad norm 记录 + KD loss 收敛曲线 |
| 知识增量 | 8GB 单卡 KD 训练可行性 + λ warmup 必要性 |

---

## 六、T4:三方同预算对照(P1)

### T4.1 A 组 baseline 训练

**配置**(同预算)

| 项 | 值 |
|----|-----|
| 数据 | COCO mini(同 T1.2)|
| 模型 | yolo-master-n(单层 2 专家)|
| epochs | 100(冒烟 1 epoch)|
| seed | {0, 1, 2} |

**命令**

```powershell
for seed in 0 1 2; do
  python train.py --config configs/f11_baseline.yaml --seed $seed \
    --project runs/f11_ablation/A --name seed_$seed
done
```

**日志与产物**

- [ ] A/seed_0/1/2 各 results.csv
- [ ] 内存/显存/FPS 日志

**结论**:⏳

---

### T4.2 B 组 表征 KD 训练

**配置**

| 项 | 值 |
|----|-----|
| KD 类型 | backbone 中间层特征对齐 |
| 教师 | DINOv2(同 T1) |
| 损失 | MSE / cosine |

**命令**

```powershell
for seed in 0 1 2; do
  python train.py --config configs/f11_repr_kd.yaml --seed $seed \
    --project runs/f11_ablation/B --name seed_$seed
done
```

**日志与产物**

- [ ] B/seed_0/1/2 各 results.csv

**结论**:⏳

---

### T4.3 C 组 Router KD 训练

**配置**

| 项 | 值 |
|----|-----|
| KD 类型 | Router 软目标蒸馏(F11 核心) |
| 教师 | q_teacher(同 T2) |

**命令**

```powershell
for seed in 0 1 2; do
  python train.py --config configs/f11_router_kd.yaml --seed $seed \
    --project runs/f11_ablation/C --name seed_$seed
done
```

**日志与产物**

- [ ] C/seed_0/1/2 各 results.csv

**结论**:⏳

---

### T4.4 同预算核对表

| 项 | A | B | C | 一致? |
|----|---|---|---|------|
| 数据 | COCO mini | 同 | 同 | ✅ |
| Epochs | 100 | 100 | 100 | ✅ |
| Batch | 8 | 8 | 8 | ✅ |
| Imgsz | 640 | 640 | 640 | ✅ |
| 增广 | default | 同 | 同 | ✅ |
| Seed | 0,1,2 | 0,1,2 | 0,1,2 | ✅ |
| GPU | RTX 5060 | 同 | 同 | ✅ |

**结论**:⏳

---

### T4.5 指标汇总

| 组 | seed=0 mAP | seed=1 mAP | seed=2 mAP | mean ± std | 95% CI |
|----|------------|-----------|-----------|-----------|--------|
| A | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ |
| B | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ |
| **C** | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ |

---

### T4.6 配对检验

**命令**

```powershell
python scripts/paired_ttest.py --A runs/f11_ablation/A --B runs/f11_ablation/B --C runs/f11_ablation/C
```

**产物**

- [ ] p 值表(A vs C, B vs C)
- [ ] bootstrap CI(1000 次重采样)

**结论**:⏳

---

### T4.7 对照表 Markdown

**产物**

- [ ] `reports/f11_three_way_comparison.md`(完整三方对照表 + 配对检验 + 路由行为对比)

**结论**:⏳(完成 → T4 完成,进入 T5)

---

### T4 增量总结

| 层 | 增量 |
|----|------|
| 代码增量 | 3 个训练配置 yaml + 对照脚本 + 配对检验脚本 |
| 实验增量 | 9 次训练(A/B/C × 3 seed)完整日志 + 对照表 + 统计检验 |
| 知识增量 | 三方同预算量化结论(C vs A/B 的 mAP/路由/负载差异)|

---

## 七、T5:路由行为分析 + 零教师证明(P1)

### T5.1 路由行为图

**配置**

| 图 | 内容 |
|----|------|
| top-k 一致率 | student vs teacher top-k 选择频次 |
| 路由熵曲线 | 训练 epoch → H_router |
| 专家负载 | top-1 选择频次直方图 |
| 切换率 | 相邻样本 top-1 变化 |

**命令**

```powershell
python scripts/routing_behavior_analysis.py \
  --ablation runs/f11_ablation/ \
  --out reports/f11_routing_behavior/
```

**产物**

- [ ] 6+ 张 PNG 图(每个指标 × 多视角)
- [ ] 数值 CSV

**结论**:⏳

---

### T5.2 JS/KL 距离

**命令**

```powershell
python scripts/compute_js_kl.py \
  --q-student runs/f11_ablation/C/student_q/ \
  --q-teacher runs/f11_q_teacher/ \
  --out reports/f11_js_kl.csv
```

**产物**

- [ ] JS/KL 数值 + 收敛曲线

**结论**:⏳

---

### T5.3 零教师移除验证

**命令**

```powershell
# 1. 导出学生模型(无教师)
python scripts/export_student.py --ckpt runs/f11_ablation/C/seed_0/best.pt --out runs/f11_student.pt

# 2. 验证教师完全移除
python scripts/verify_no_teacher.py --student runs/f11_student.pt
```

**产物**

- [ ] 学生模型文件(参数量 = baseline - 教师)
- [ ] verify 报告(无 dino / 无 teacher 引用)
- [ ] 推理延迟 / 显存 / 体积对比表

**结论**:⏳(✅ → T5.4)

---

### T5.4 推理期精度对比

**命令**

```powershell
python scripts/eval_student.py --student runs/f11_student.pt --data coco8.yaml
```

**产物**

- [ ] 移除教师后 mAP50-95(应与训练期一致,Δ < 0.1)

**结论**:⏳(完成 → T5 完成)

---

### T5 增量总结

| 层 | 增量 |
|----|------|
| 代码增量 | 4 个分析/验证脚本 |
| 实验增量 | 6+ 张行为图 + 零教师移除证明 + 推理期精度对比 |
| 知识增量 | 路由稳定性量化结论 + 零教师依赖可验证证据 |

---

## 八、T6:扩展实验(P2 可选)

> 选 ≥2 个方向执行(任务书 P2 明示)。

### T6.1 多层路由 KD 扩展

**配置**

| 项 | 值 |
|----|-----|
| layers | 1 → 3 |
| experts | 2 |

**命令**

```powershell
python train.py --config configs/f11_router_kd_l3.yaml --seed 0
```

**产物**

- [ ] 单层 vs 三层 mAP 趋势 + 计算开销对比

**结论**:⏳

---

### T6.2 MoE/MoT 路由扩展

**配置**

| 项 | 值 |
|----|-----|
| 路由类型 | ES-MoE / MoT 异构 |

**命令**

```powershell
python train.py --config configs/f11_router_kd_mot.yaml --seed 0
```

**产物**

- [ ] 不同路由族的 KD 效果对比

**结论**:⏳

---

### T6.3 小样本/跨域

**配置**

| 项 | 值 |
|----|-----|
| 数据 | VisDrone 子集 100/500/1000 |

**命令**

```powershell
python train.py --config configs/f11_router_kd_visdrone.yaml --data visdrone_sub.yaml
```

**产物**

- [ ] 跨域 APs 提升数据

**结论**:⏳

---

### T6.4 教师对比(DINOv2 vs DINOv3)

**命令**

```powershell
python train.py --config configs/f11_router_kd_dinov3.yaml --seed 0
```

**产物**

- [ ] 两种教师蒸馏效果对比

**结论**:⏳

---

### T6.5 负结果机制分析(若触发)

**配置**

| 维度 | 分析 |
|------|------|
| 教师软目标质量 | H_norm / 切换率 / 与 student 余弦相关 |
| 专家同质化 | 专家输出互信息 |
| 温度/权重 | 扫描 τ, λ |
| 路由坍塌 | 单专家选中率随 epoch 变化 |

**命令**

```powershell
python scripts/analyze_negative_results.py \
  --ablation runs/f11_ablation/ \
  --out reports/f11_negative_analysis.md
```

**产物**

- [ ] 负结果定位报告(任务书允许并要求证据链)

**结论**:⏳

---

### T6 增量总结

| 层 | 增量 |
|----|------|
| 代码增量 | 多份扩展配置 + 负结果分析脚本 |
| 实验增量 | 多层/MoT/跨域/教师对比 各 1+ seed |
| 知识增量 | 路由 KD 泛化性 + 教师软目标质量影响分析 |

---

## 九、增量汇总表

> 提交结项时使用。增量层 = 代码 / 实验 / 知识 三层。

| 任务 | 级别 | 状态 | 代码增量 | 实验增量 | 知识增量 |
|------|------|------|---------|---------|---------|
| T0 基线复现 | Smoke | ⏳ | ⏳ | ⏳ | ⏳ |
| T1 特征缓存 | P0 | ⏳ | scripts/cache_teacher_features.py + manifest | 100 张 DINOv2 缓存 | 8GB 离线缓存方法论 |
| T2 q_teacher | P0/P1 | ⏳ | 5 个脚本 + 配置 | 100 张 q_teacher 分布 | τ/ε/掩码 影响曲线 |
| T3 KD 闭环 | P0 | ⏳ | RouterKDLoss + 注册 + smoke | 1 epoch KD 训练 + grad 记录 | λ warmup 必要性 |
| T4 三方对照 | P1 | ⏳ | 3 配置 + 检验脚本 | 9 次训练 + 配对检验 | 三方量化结论 |
| T5 行为+零教师 | P1 | ⏳ | 4 个分析脚本 | 6+ 图 + 移除证明 | 零教师可验证证据 |
| T6 扩展 | P2 | ⏳ | 多配置 + 负分析 | 多层/MoT/跨域/教师 | 泛化性 + 负结果机制 |

---

**维护者**:张伟林(Zviolin)｜**最后更新**:2026-08-23
**下一步**:8/24 冒烟后按 T0→T6 逐项填写实际数值;每项提交 PR/日志/指标表四件套